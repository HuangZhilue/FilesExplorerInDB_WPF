<UserControl
    x:Class="FilesExplorerInDB_WPF.View.IndexTabView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:local="clr-namespace:FilesExplorerInDB_WPF.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:FilesExplorerInDB_WPF.ViewModel"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="#FFE5E5E5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <GroupBox
            Grid.Column="2"
            HorizontalContentAlignment="Center"
            Header="系统设置" />
        <Button
            x:Name="Button_Setting"
            Grid.Column="2"
            Margin="5,20,5.4,4.8"
            Background="{x:Null}"
            BorderBrush="{x:Null}"
            Content="设置">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Click">
                    <i:InvokeCommandAction Command="{Binding CommandSettings, Mode=OneWay, Source={x:Static viewModel:ExplorerItemsVM.GetInstance}}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Button>
        <GroupBox
            Grid.Column="1"
            HorizontalContentAlignment="Center"
            Header="刷新" />
        <Button
            x:Name="Button_Refresh"
            Grid.Column="1"
            Margin="5,20,5.4,4.8"
            Background="{x:Null}"
            BorderBrush="{x:Null}"
            Command="{Binding CommandRefreshAll, Mode=OneWay}"
            Content="刷新" />
    </Grid>
</UserControl>
